{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "next start --hostname 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "always"
  }
}
